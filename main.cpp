#include <iostream>
#include "graph.h"
#include "bst.h"
#include "linkedlist.h"
#include "sort.h"
using namespace std;

#include <ctime>
#include <cstdio>

int main(int argc, char** argv) {
    int nPoints;
    if (argc > 1) {
        nPoints = stoi(argv[1]);
    }

    cout << "Amount of points: " << nPoints << endl;

    //graph dots;
    //dots.fill(nPoints);

}
